{% extends 'baseAdmin.html.twig' %}

{% block title %}Edit Option{% endblock %}

{% block main %}
    <!-- Content Wrapper. Contains page content -->
    <div class="content-wrapper">
        <section class="content-header">
            <h1>
                Modification d'une option
            </h1>
            <ol class="breadcrumb">
                <li><a href="{{ path ('Dashboard') }}"><em class="fa fa-dashboard"></em>Tableau de bord</a></li>
                <li><a href="#">liste option</a></li>
                <li class="active"><a href="{{ path ('listeSlider') }}">éditer</a></li>
            </ol>
        </section>

        <!-- Main content -->
        <section class="content">
            <div class="row">
                <div class="col-xs-12">
                    <div class="box">
                        <div class="box-header">
                        </div>
                        <!-- /.box-header -->
                        <div class="box-body">
                            {{ form_start(form) }}
                            {{ form_widget(form) }}
                            <button type="submit" class="btn btn-success">Éditer</button>
                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>
                <!-- /.col -->
            </div>
            <!-- /.row -->
        </section>
        <!-- /.content -->
    </div>

{% endblock %}
